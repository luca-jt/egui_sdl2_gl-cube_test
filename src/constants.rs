/// width of the whole window
pub const SCREEN_WIDTH: u32 = 1200;
/// height of the whole window
pub const SCREEN_HEIGHT: u32 = 700;

/// width of the rendered panel inside the GUI
pub const PIC_WIDTH: usize = 600;
/// height of the rendered panel inside the GUI
pub const PIC_HEIGHT: usize = 600;
